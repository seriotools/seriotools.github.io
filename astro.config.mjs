// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // Static output — ideal for GitHub Pages (seriotools.github.io)
    output: 'static',
    site: 'https://seriotools.github.io',
});
